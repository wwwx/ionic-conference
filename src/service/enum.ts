export enum ConferenceType {
  HAS_NOT_START,
  PROCESSING,
  FINISHED,
}

export enum Role {
  HOST = 'host',
  RECORDED = 'recorder',
}

export enum RecordType {
  HAS_NOT_START,
  PROCESSING,
  PUBLISHED,
}
